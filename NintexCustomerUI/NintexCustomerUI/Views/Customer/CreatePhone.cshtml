@using NintexCustomerUI.Models
@model CustomerPhone
@{
    ViewData["Title"] = "Add New Phone";
}

<h2>Add New Phone number</h2>

@Html.ActionLink("Back To Details", "Detail", new { id = Model.Cust_ID })
@using (Html.BeginForm())
{
    @Html.HiddenFor(model => model.Cust_ID)
    <div class="form-horizontal">
        <div>
            <table>
                <tr>
                    <td>
                        Phone Number :
                    </td>
                    <td>
                        @Html.TextBoxFor(model => model.Phone_Number, new { @Value = ""})
                    </td>
                </tr>
                <tr>
                    <td>
                        Is Active :
                    </td>
                    <td>
                        @Html.CheckBoxFor(model => model.Active)
                    </td>
                </tr>
            </table>
        </div>
        <div>
            <div>
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

